# This file was auto-generated by the Firebase CLI
# Modified to use Yarn instead of npm to avoid platform/engine issues
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'yarn'

      - name: Install Yarn
        run: npm install -g yarn@1.22.21

      - name: Install dependencies
        run: yarn install --ignore-platform

      - name: Build project
        run: yarn build --base-href=/

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_INTERNATIONALISATION_MOU }}
          channelId: live
          projectId: internationalisation-mou
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
